<%= form_for(@purchase_option) do |f| %>
  <% if @purchase_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_option.errors.count, "error") %> prohibited this purchase_option from being saved:</h2>

      <ul>
      <% @purchase_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :video_quality %><br>
    <%= f.text_field :video_quality %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
